[pytest]
addopts = -ra
testpaths = tests
pythonpath = . src
asyncio_default_fixture_loop_scope = function
markers =
    integration: Тесты, требующие исторических данных/бэктеста; в probe-ветках CI пропускает их.
filterwarnings =
    ignore::DeprecationWarning
