# 🛡️ Binance Futures Rebalancer: Progress & Operational Log

Асинхронная экосистема для автоматического поддержания рыночно-нейтрального портфеля. 
**Текущая фаза:** Swarm Automation + Value Rebalancing (Final RC).
**Статус:** 100% Верифицировано на длинной истории + Автономная ротация.

---

## 🗓 26 апреля 2026 (Ночь): Защита капитала и Прозрачность отчетности (Critical Fix)

### ✅ Ключевые достижения:
1.  **Active Capital Siphoning Guard (Surgical Fix):** Исправлено правило вывода прибыли. Теперь система блокирует перевод средств в SAFE, если **активный капитал** (Total TPV минус уже накопленный SAFE) ниже начального. Это предотвращает "вымывание" рабочего тела бота, если он находится в просадке, даже если суммарный профит (с учетом сейфа) положителен.
2.  **Baseline Integrity (initial_capital):** Добавлена поддержка параметра `initial_capital` в `config.json`. Теперь боты всегда "помнят" свой первоначальный вклад (например, $10,000), даже если были перезапущены в момент глубокой просадки. 
3.  **Telegram Transparency 2.0:** Полностью переработан формат отчетов в Telegram для исключения "самообмана" системы:
    -   Показатель `💰 TPV` заменен на `💰 Balance` (Active Capital). Он показывает реальное рабочее тело бота (Total TPV - SAFE).
    -   `📈 Net Profit` теперь рассчитывается строго относительно `initial_capital` из конфига. Это дает честную картину прибыли/убытка по всему рою.
4.  **Calculator Recovery Fix:** Исправлен баг в `calculator.py`, из-за которого в режиме просадки (Recovery Mode) информация о накопленном SAFE временно стиралась из расчетов. Теперь данные о резервах сохраняются корректно в любых фазах рынка.
5.  **Global State Migration:** Проведена миграция всех файлов `state_*.json`. Параметры `initial_tpv` и `reference_tpv` принудительно синхронизированы с реальным капиталом ($10,000.0) для всех активных ботов.

---

## 🗓 26 апреля 2026 (Вечер): Безопасность и Альфа-валидация

### ✅ Ключевые достижения:
1.  **Ticker Sanitization (ASCII Guard):** Введена жесткая фильтрация тикеров в `rank_tickers.py`. Теперь система полностью игнорирует символы вне диапазона ASCII.
2.  **Anti-Madness Filter:** В сканер добавлена логика исключения одного самого "бешеного" тикера (с максимальным `MAX SPURT%`).
3.  **Alpha-Gate Validation:** В `supervisor.py` внедрена проверка `alpha_vs_hodl > 0` перед добавлением тикера в рой.
4.  **Profit Milestone:** Фиксация прибыли в **+4.28%** ($4,281.56) за 17 часов работы обновленного роя.

---

## 🛠 Команды управления (Cheat Sheet)

### 🚀 Запуск и Автоматизация
- `pm2 start supervisor_service.py --name supervisor-service` — Главная команда управления роем.
- `pm2 logs supervisor-service` — Просмотр логов ротации.
- `pm2 status` — Статус всех ботов.

---

## 🗓 27 апреля 2026 (Ночь): Micro-Deposit Stress Test

### ✅ Ключевые достижения:
1.  **Capital Scaling Fix:** Исправлен `main.py` для корректной инициализации "бумажного" баланса из параметра `initial_capital` в `config.json`. Устранена жесткая привязка к $10,000.
2.  **Full Reset executed:** Система полностью обнулена через `full_reset.py`. Удалены старые состояния и логи для чистоты эксперимента.
3.  **Swarm Launch (39 USDT):** Запущен рой из 10 ботов в Paper Mode, где каждый оперирует виртуальными 39 USDT. Это позволит проверить эффективность стратегии при высоком влиянии `min_notional` (6 USDT).

---

## Completed Tasks
- [x] **Адаптация под малый депозит (27.04.2026):**
  - [x] Фикс `main.py` (приоритет `initial_capital` над дефолтными 10к).
  - [x] Исправление `full_reset.py` (защита конфига от затирания).
  - [x] Рестарт всей системы с базой 39 USDT.

## Next Steps
- [ ] **Анализ "Гранулярности" (Завтра):** Оценка того, как часто боты совершают сделки при лимите 6 USDT на 39 USDT депозита.
- [ ] **Выбор фаворита:** Отбор 1-2 тикеров с лучшей "альфой" для перевода в Real Mode.
- [ ] **Real Mode Launch:** Переход к реальной торговле на Binance Futures с депозитом 39 USDT.

---
*Log updated: 26 апреля 2026 (20:00)*
