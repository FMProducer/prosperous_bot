# Останавливаем выполнение при любой ошибке
set -e

echo "--- Starting Initial Setup ---"

# 1. Установка зависимостей проекта
echo "--- Installing Python dependencies ---"
pip install -r requirements.txt

# 2. Установка самого проекта в режиме редактирования (ВАЖНЫЙ ШАГ)
echo "--- Installing project in editable mode ---"
pip install -e .

# 3. Вывод ключевой информации о среде для логов
echo "--- Environment Verification ---"
python --version
pip --version
pytest --version

# 4. Запуск базовой проверки тестами
echo "--- Running initial test suite ---"
pytest -m "not integration" --cov

echo "--- Initial Setup Finished Successfully ---"